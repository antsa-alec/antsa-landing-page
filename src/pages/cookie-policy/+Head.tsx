import { PageHead } from '../pageHead';

export default function Head() {
  return (
    <PageHead
      title="Cookie and Tracking Policy — ANTSA"
      description="The live register of cookies, pixels, tags and third-party SDKs used across ANTSA platforms, including purposes, providers, duration and consent category."
      path="/cookie-policy"
    />
  );
}
